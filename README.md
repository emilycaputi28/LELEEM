# LELEEM

## Grupo

- Emily Caputi 
- Leticia Feitoza 
- Leticia Tatesuji

---

## Objetivo do Algoritmo

O algoritmo consiste em uma calculadora funcional de quatro operações básicas. Seu objetivo é receber dois números reais informados pelo usuário, permitir a escolha da operação desejada (soma, subtração, multiplicação ou divisão) e exibir o resultado final da operação na tela por meio de funções específicas para cada cálculo.

---

## Aplicações

- **Integrante 1 (Emily Caputi):** Responsável pelo desenvolvimento da estrutura inicial do algoritmo em Portugol e pela criação e estruturação inicial do arquivo `README.md`.
- **Integrante 2 (Leticia Feitoza):** Responsável pelo desenvolvimento e implementação das funções das operações de soma e subtração, além de realizar atualizações no arquivo `README.md`.
- **Integrante 3 (Letícia Tatesuji):** Responsável pelo desenvolvimento e implementação das funções das operações de multiplicação e divisão, além de realizar atualizações no arquivo `README.md`.

---

## Atualizações

 - **Integrante 1**: Desenvolvimento da estrutura inicial, criação do ReadMe.
 - **Integrante 2**: Adicionando operações de soma e subtração, atualização do ReadMe.
 - **Integrante 3**: Adicionando operações de multiplicação e divisão, atualização do ReadMe.
 - **Integrante 1**: Atualização final do ReadMe.

---

## Registro de Edições no README.md

- **Emily Caputi:** Criou o arquivo e fez a formatação final.
- **Leticia Feitoza:** Atualizou o progresso após a entrega de suas funções.
- **Letícia Tatesuji:** Atualizou o progresso após a entrega de suas funções.

---

## Comandos Utilizados Por Cada Integrante

 - **Integrante 1**: git config, ls -al ~/.ssh, rm -f ~/.ssh/id_rsa*, ssh-keygen -t rsa -b 4096 -C, eval "$(ssh-agent -s)", ssh-add ~/.ssh/id_rsa, clip < ~/.ssh/id_rsa.pub, ssh -T git@github.com, git clone, cd, touch calculadora.por, git add, git commit -m, git push, code ., git status, git pull;

 - **Integrante 2**: git config, ssh-keygen -t rsa -b 4096 -C, eval "$(ssh-agent -s)", ssh-add ~/.ssh/id_rsa, clip < ~/.ssh/id_rsa.pub, ssh -T git@github.com, git clone, cd, code ., git pull, git add, git commit -m, git push;

 - **Integrante 3**: git config, ssh-keygen -t rsa -b 4096 -C, eval "$(ssh-agent -s)", ssh-add ~/.ssh/id_rsa, clip < ~/.ssh/id_rsa.pub, ssh -T git@github.com, git clone, cd, code ., git pull, git add, git commit -m, git push.

---

## Resumo dos Commits:

 1. **Commit 1:** Criação e integração inicial do `README.md` (Integrante 1)
 2. **Commit 2:** Integração da estrutura básica do algoritmo (Integrante 1)
 3. **Commit 3:** Integração das operações de soma e subtração + Atualização do `README.md` (Integrante 2)
 4. **Commit 4:** Integração das operações de multiplicação e divisão +   Atualização do `README.md` (Integrante 3)
 5. **Commit 5:** Revisão e atualização final do `README.md` (Integrante 1)